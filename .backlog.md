- For Writable sequences, take a look at https://github.com/modular/modular/blob/95429ae83a78d120783802c19ee856653c64b621/mojo/stdlib/std/format/_utils.mojo#L84
- Try running `mojo doc src/immu -o some/path.json` (requires all source files to compile) and looking at the results. It should include exactly the public facing types, functions and methods, constants etc. If the format is easy to parse, it could be used to test that everything that should be exposed is, and everything that shouldn't isn't. (When running unit-tests, we have access to package internal entities, so there's no check that things have the right accessbility status.)